struct A {
    a: i32,
}

struct B {
    a: A,
}

struct C {
    b: B,
}

struct D {
    c: C,
}

union Option {
    Some(string),
    None,
}

union Result {
    Ok(string),
    Err(string),
}

D d = D { c: C { b: B { a: A { a: 5 } } } }
