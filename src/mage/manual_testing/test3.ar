proto P {
    fun f(): String;
    fun m(self: Self): String;
}

enum E {
    A { value: String },
    B,
}

imp P for E {
    fun f(): String => {
        "Hello, world!"
    }

    fun m(self: Self): String => {
        self match
        | A { value } => value,
        | B => "Hello, B!",
    }
}

// S::f()
// print(S::f());
let s: E = E::A { value: "Hello, A"};

s:m
