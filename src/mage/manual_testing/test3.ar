proto P {
    fun f(): string;
    fun m(self: Self): string;
}

struct S;

imp P for S {
    fun f(): string => {
        "Hello, world!"
    }

    fun m(self: Self): string => {
        "Hello, world!"
    }
}

print(S::f());
// let s = S {};

// print(s:m());
