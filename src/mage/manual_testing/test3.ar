fun map(x: [int], mapper: fun(int): int): [int] => {
  let mut result: [int] = [];

  for i in x => {
    result += mapper(i);
  }

  result
}

let nums = [1, 2, 3, 4, 5];

let doubled = nums:map(|x| x*2)
